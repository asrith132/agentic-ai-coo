[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: Unit tests (no external deps)
    integration: Integration tests (mocked DB/API)
    e2e: End-to-end workflow tests
